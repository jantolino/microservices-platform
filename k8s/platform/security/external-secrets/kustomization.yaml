apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: security

helmCharts:
  - name: external-secrets
    repo: https://charts.external-secrets.io
    version: 2.4.0
    releaseName: external-secrets-2.4.0
    namespace: security
    valuesFile: external-secrets-values.yaml

resources:
  - cluster-secret-store.yaml