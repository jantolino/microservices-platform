apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cert-manager

resources:
  - cluster-issuer.yaml
  - cert-manager-service-monitor.yaml

helmCharts:
  - name: cert-manager
    repo: https://charts.jetstack.io
    version: 1.18.2
    releaseName: cert-manager-v1-18-2
    namespace: cert-manager
    valuesFile: cert-manager-values.yaml